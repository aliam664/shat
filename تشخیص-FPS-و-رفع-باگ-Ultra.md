# تشخیص افت FPS + رفع باگ‌های پریست Ultra
### (بدون تغییر گرافیک — فقط اصلاح باگ‌ها)

> پروژه: `shat` · تاریخ: ۱۳ سپتامبر ۲۰۲۶
> محدوده‌ی تغییرات: فقط پریست‌های **Ultra** (ویدیو + CSP) و نام فایل فیلتر

---

## ۱. چرا FPS پایین است؟ (تشخیص دو بخشی)

افت FPS در این مجموعه از **دو منبع** می‌آید: (الف) سنگینی عمدی پریست Ultra و (ب) چند باگ که FPS را بی‌جهت محدود می‌کردند.

### الف) سنگینی عمدی (باید بماند — این «اولترا» بودن است)

| عامل | مقدار در Ultra | اثر بر FPS |
|---|---|---|
| **Render Scale** | `SCALE=1.5` (رندر ۱۵۰٪) | سنگین‌ترین عامل — رزولوشن داخلی ۲.۲۵ برابر می‌شود (در 1080p معادل 2880×1620) |
| **MSAA** | `AASAMPLES=8` (۸x) | روی رندر ۱۵۰٪، هزینه‌ی پُرشدت زیادی دارد |
| **سایه‌ها** | `SHADOW_MAP_SIZE=4096` | نقشه‌ی سایه‌ی 4K |
| **انعکاس** | `FACES_PER_FRAME=6` + `SIZE=2048` | انعکاس کاملِ هر ۶ وجه مکعب در هر فریم |
| **SSGI** | فعال | روشنایی سراسری Screen-Space (بسیار سنگین) |
| **SSLR** | `STEPS_HIZ=300` | انعکاس‌های Screen-Space با گام ردیابی بالا |
| **HBAO + SSAO + ASSAO** | هر سه فعال | چند پاس سایه‌روشن محیطی |
| **نور حجمی** | `SCALE=0.32` | نور حجمی در هوا |
| **PURE: ابرهای حجمی ۳بعدی** | `clouds_raymarching.clouds=true`, `shadows=true` | سنگین‌ترین بخش PURE (رای‌مارچینگ + سایه‌ی ابر) |
| **PURE: بافت آسمان 16K** | `clouds2D.set=default_16k` | فشار VRAM |
| **بقیه** | WORLD_DETAIL=5، SMOKE=5، آینه 2048 HQ | — |

> این موارد **تغییر داده نشده‌اند** (بنا به درخواست «گرافیک همان بماند»). اگر بعد از رفع باگ‌ها FPS همچنان زیر هدف بود، این‌ها اهرم‌های کاهش هستند (اول‌ویت پیشنهادی در بخش ۴).

### ب) باگ‌هایی که FPS را بی‌جهت پایین می‌آوردند (رفع شد)

| # | باگ | اثر | اصلاح |
|---|---|---|---|
| 1 | `FPS_CAP_MS=16.666...` | **قفل سخت روی ۶۰ فریم** در حالی که مانیتور ۱۲۰ هرتز است → مهم‌ترین دلیل «پایین‌بودن» FPS | `FPS_CAP_MS=0` (بدون سقف) |
| 2 | `MAXIMUM_FRAME_LATENCY=6` | صف ۶ فریمی GPU → تاخیر ورودی بالا و افت FPS مؤثر | `0` (هم‌راستا با بقیه‌ی پریست‌ها) |
| 3 | `SELECT_ADAPTER=NVIDIA?` | مقدار ناقص؛ در سیستم‌های هیبریدی می‌تواند باعث انتخاب GPU اشتباه (iGPU) شود | حذف (انتخاب خودکار) |

### ج) باگ‌های صحتِ رندر (بر FPS اثر ندارند ولی تصویر را خراب/ناهماهنگ می‌کردند — رفع شد)

| # | باگ | اثر | اصلاح |
|---|---|---|---|
| 4 | نبود `DISABLE_LEGACY_HDR=1` | پریست‌های CSP روی **Pure LCS** تنظیم‌اند؛ با فعال‌ماندن HDR قدیمی، نوردهی مضاعف/رنگ شسته | افزوده شد |
| 5 | نبود `AAQUALITY=0` | حالت رزولوشن MSAA به پیش‌فرض برمی‌گشت | افزوده شد (بالاترین کیفیت، هم‌راستا با بقیه) |
| 6 | نبود `FXAA=0` در `[EFFECTS]` | احتمال FXAA مضاعف (بلور اضافه) | افزوده شد |
| 7 | `_EXT_PLACEMENT=...` | داده‌ی جای‌گذاری پنجره از سیستم چندمانیتوره‌ی سازنده | حذف شد |
| 8 | `FILE_OP_HOOK=C:/Users/illvd/...cspdecryptexample.dll` | هوک DLL محلی سازنده (در سیستم شما وجود ندارد) | حذف شد |
| 9 | نام فایل فیلتر با مرجع پریست‌ها نمی‌خواند | `UhmFilter.ini` در برابر `FILTER=NFS 2015 Edited By Uhm` → فیلتر لود نمی‌شد | فایل به `NFS 2015 Edited By Uhm.ini` تغییر نام یافت |

---

## ۲. جزئیات دقیق تغییرات (قبل → بعد)

### فایل `4 ~ Video_Settings/𝐔𝐡𝐦-𝐯𝐢𝐝𝐞𝐨{Ultra}.cmpreset`

**`[EFFECTS]`**
```
- MOTION_BLUR=0            + FXAA=0
                            + MOTION_BLUR=0
```

**`[VIDEO]`**
```
- AASAMPLES=8              + AAQUALITY=0
                            + AASAMPLES=8
- ANISOTROPIC=16           + ANISOTROPIC=16
- FPS_CAP_MS=16.6666666666667  + DISABLE_LEGACY_HDR=1
                            + FPS_CAP_MS=0
- ...
- WIDTH=1920
- _EXT_PLACEMENT=px0_py0_...1080   (حذف شد)
```

**`[DX11]`**
```
- MAXIMUM_FRAME_LATENCY=6  + MAXIMUM_FRAME_LATENCY=0
```

> `WORLD_DETAIL=5`, `AASAMPLES=8`, `SHADOW_MAP_SIZE=4096`, `CUBEMAP=2048/6`, `GLARE=5`, `DOF=5` و... **دست نخورده‌اند** (گرافیک بدون تغییر).

### فایل `1 ~ CSP_Settings/𝐔𝐡𝐦-𝐂𝐬𝐩{Ultra}.ini`

```
- FILE_OP_HOOK=C:/Users/illvd/source/repos/csp_decrypt_example/x64/Release/cspdecryptexample.dll,FileOpHook
- SELECT_ADAPTER=NVIDIA?
```

> `RENDER_SCALE SCALE=1.5`، `SSGI`، `SSLR STEPS_HIZ=300`، `HBAO/SSAO/ASSAO`، `VOLUMETRIC_LIGHTS` و بقیه‌ی افکت‌های اولترا **دست نخورده‌اند**.

### فایل `2 ~ PPFilter/UhmFilter.ini`

```
→ تغییر نام به:  2 ~ PPFilter/NFS 2015 Edited By Uhm.ini
```
(محتوای فیلتر بدون هیچ تغییری است؛ فقط نام با `FILTER=` داخل پریست‌های ویدیو هماهنگ شد تا فیلتر واقعاً لود شود.)

---

## ۳. نتیجه‌ی مورد انتظار

- ❌ → ✅ حذف سقف ۶۰ فریم: FPS دیگر به ۶۰ قفل نیست (با مانیتور ۱۲۰ هرتز می‌تواند بالاتر برود).
- ❌ → ✅ حذف صف ۶ فریمی: پاسخ‌دهی ورودی بهتر و فریم‌تایم روان‌تر.
- ❌ → ✅ تصحیح مسیر HDR برای Pure LCS: رنگ و نوردهی درست.
- ❌ → ✅ فیلتر NFS 2015 واقعاً لود می‌شود.

> توجه: خودِ «سنگینی» اولترا (رندر ۱۵۰٪ + MSAA 8x + SSGI + SSLR + ابرهای حجمی) باقی است و عمدی است. اگر هدفِ FPS مشخصی دارید، بخش بعد را ببینید.

---

## ۴. اگر بعد از رفع باگ‌ها FPS باز هم کم بود (اهرم‌های اختیاری — اعمال نشده)

به‌ترتیبِ «بیشترین سود با کمترین افت بصری»:

1. `CSP → Graphics Adjustments → Render Scale → Scale = 1` (حذف رندر ۱۵۰٪ — بزرگ‌ترین سود FPS، محسوس‌ترین تفاوت در شارپنس).
2. `Video → MSAA = 4x` (8x→4x روی رندر ۱۵۰٪ بسیار سنگین است).
3. `CSP → Weather FX → Clouds → Raymarching → off` (خاموش‌کردن ابرهای ۳بعدی).
4. `Video → Reflections → Faces per frame = 2` (6→2).
5. `CSP → Extra FX → SSGI = off` (و در صورت نیاز SSLR).
6. `Video → Shadows = 2048` (4096→2048).
7. `Pure Config → Clouds → set = default_8k` (16K→8K برای VRAM کمتر).

> این‌ها صرفاً پیشنهاد هستند و عمداً اعمال نشده‌اند تا «گرافیک همان بماند».
